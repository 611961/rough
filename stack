#include<iostream>
#include<stack>
using namespace std;

//wkt stack itself contains in reverse order

int main(){
	stack<char>ss; 
	string s;
	cin>>s;
	for(char c : s){
		ss.push(c);
	}
	while(!ss.empty()){
		cout<<ss.top();
		ss.pop();
	}
}
