#include<iostream>
using namespace std;

int main(){
	int n=4;
	int a[n]={3,6,7,11};
	int hrs=8;
	int efficiency= 3;              // 3 bnns/hr
	int maxi=-1e9;
	for(int i=0;i<n;i++){
		maxi =max(maxi,a[i]);
	}
		int totalh=0;
		for(int i=0;i<n;i++){
			totalh += a[i]/
		}
		int l=1,h=max;
		while(l<=h){
			int mid=l+(h-l)/2;
			if(totalh<=h){
				h=mid-1;
			}
			else{
				l=mid+1;
			}
		}
}
